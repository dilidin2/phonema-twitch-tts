# Core ML/Audio (VoxCPM2 — GPU model)
voxcpm
numpy

# Pocket TTS (CPU model — installa solo se usi model_type = "cpu_model")
# pocket-tts
# scipy

# Web/API
fastapi
uvicorn[standard]
python-multipart

# Twitch integration
twitchAPI>=4.5.0
aiohttp

# Configuration
pyyaml
python-dotenv

# Logging
loguru

# Audio processing
pasimple
soundfile
